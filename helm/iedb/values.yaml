# Default values for iedb

replicaCount: 1

# Update strategy for the Deployment. Defaults to Recreate because iedb uses a
# ReadWriteOnce PVC — RollingUpdate deadlocks when replicaCount=1: the new pod
# can't attach the volume until the old pod terminates, but RollingUpdate waits
# for the new pod to be ready first.
# Switch to RollingUpdate only when using shared object storage (s3/azure/minio)
# with iedb Enterprise clustering (e.g. multiple stateless reader replicas).
updateStrategy:
  type: Recreate
  # For RollingUpdate, tune these to control availability during rollout:
  # rollingUpdate:
  #   maxSurge: 1
  #   maxUnavailable: 0

image:
  repository: ghcr.io/Mengdal/IotEdgeDB
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  fsGroup: 1000

securityContext:
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1000

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: iedb.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}
  # limits:
  #   cpu: 2000m
  #   memory: 4Gi
  # requests:
  #   cpu: 1000m
  #   memory: 2Gi

# iedb-specific configuration
iedb:
  # Storage backend: local, s3, minio
  storageBackend: local
  # Additional environment variables
  env: []
    # - name: IEDB_AUTH_ENABLED
    #   value: "true"

# Persistent storage for iedb data
persistence:
  enabled: true
  # storageClass: "-"
  accessMode: ReadWriteOnce
  size: 10Gi
  # existingClaim: ""

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}
