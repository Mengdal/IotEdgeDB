apiVersion: v1
kind: Service
metadata:
  name: {{ include "iedb.fullname" . }}
  labels:
    {{- include "iedb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iedb.selectorLabels" . | nindent 4 }}
