{{- if .Values.compactor.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "iedb-enterprise.compactorName" . }}
  labels:
    {{- include "iedb-enterprise.roleLabels" (dict "ctx" . "role" "compactor") | nindent 4 }}
spec:
  serviceName: {{ include "iedb-enterprise.compactorHeadlessName" . }}
  replicas: {{ .Values.compactor.replicas }}
  selector:
    matchLabels:
      {{- include "iedb-enterprise.roleSelectorLabels" (dict "ctx" . "role" "compactor") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "iedb-enterprise.roleLabels" (dict "ctx" . "role" "compactor") | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: iedb
          image: {{ include "iedb-enterprise.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: 8000
            - name: coordinator
              containerPort: 9100
          env:
            {{- include "iedb-enterprise.commonClusterEnv" . | nindent 12 }}
            - name: IEDB_CLUSTER_ROLE
              value: "compactor"
            - name: IEDB_CLUSTER_ADVERTISE_ADDR
              value: "$(POD_NAME).{{ include "iedb-enterprise.compactorHeadlessName" . }}.{{ .Release.Namespace }}.svc.cluster.local:9100"
            - name: IEDB_CLUSTER_SEEDS
              value: {{ include "iedb-enterprise.writerSeeds" . | quote }}
            - name: IEDB_COMPACTION_ENABLED
              value: "true"
            {{- include "iedb-enterprise.storageEnv" . | nindent 12 }}
            {{- with .Values.compactor.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: data
              mountPath: /app/data
            {{- if .Values.cluster.tls.enabled }}
            - name: tls
              mountPath: /etc/iedb/tls
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.compactor.resources | nindent 12 }}
      {{- if .Values.cluster.tls.enabled }}
      volumes:
        - name: tls
          secret:
            secretName: {{ .Values.cluster.tls.existingSecret | quote }}
      {{- end }}
      {{- include "iedb-enterprise.roleScheduling" (dict "ctx" . "role" "compactor") | nindent 6 }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- $sc := .Values.compactor.persistence.storageClass | default .Values.storage.local.storageClass }}
        {{- if $sc }}
        storageClassName: {{ $sc | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.compactor.persistence.size | quote }}
{{- end }}
